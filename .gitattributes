# Normalize line endings to LF in the repo (so Jetson clone gets LF)
* text=auto eol=lf

# Force binary — never touch line endings
*.tflite binary
*.onnx   binary
*.engine binary
*.pt     binary
*.so     binary
